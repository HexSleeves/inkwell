# Engineering Spikes

This directory holds short engineering spikes for Inkwell design and implementation decisions.

- **001** — Browser Login UI — Designs a browser login page that exchanges scoped author tokens for an `inkwell_session` cookie.
- **002** — Write-Audit History API — Designs a read-only document history endpoint over the append-only `write_audit` trail.
